<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partials/head.ejs') %>
    <style>
        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-evenly;
        }
        img {
            display: flex;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            margin-left: 15px;
        }
        
        ul {
            width: 250px;
        }
        .animalInfo {
            display: block;
        }
    </style>
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <main>
        <div class="container">
            <% for (animal of animals) { %>
                <ul>
                    <li class="animalInfo"><img src="<%= animal.img %>"></li>
                    <li class="animalInfo"><a href="/<%= animal._id %>"><h2>Species: <%= animal.species %></h2></a></li>
                    <li class="animalInfo">Origin Location: <%= animal.location %></li>
                    <li class="animalInfo">Life Expectancy: <%= animal.lifeExpectancy %> Years</li>
                    <li class="animalInfo">extinct: <%= animal.extinct %></li>
                </ul>
            <% } %> 
        </div> 
    </main>
</body>
</html>